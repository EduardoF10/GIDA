# Express API origin. Leave empty in local Vite (proxies /api to http://localhost:4000).
VITE_API_URL=

# Supabase (Settings → API in dashboard) — used for auth and admin uploads
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
